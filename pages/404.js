import Head from 'next/head'
import React, { useContext } from 'react'
import ThemeContext from '../context/Theme'
import Header from '../components/Header'
import Layout from '../components/Layout'
import Link from 'next/link'

const Error404 = () => {
  const { DarkTheme } = useContext(ThemeContext)
  return (
    <div className={`${DarkTheme ? 'bg-bodyDark' : 'bg-bodyLight'} min-h-screen`} >
      <Head>
        <title>REST Countries</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Header />
      <Layout>
        <div className='flex items-center justify-center flex-col mt-40'>
          <h1 className={`${DarkTheme ? 'text-white' : 'text-textDark'} text-4xl md:text-9xl text-center font-bold`}>NOT FOUND</h1>

          <p className={`${DarkTheme ? 'text-white' : 'text-textDark'} text-center text-4xl md:text-6xl`}>Ooops 💔 Error 404</p>

          <Link href='/' className={`${DarkTheme ? 'bg-containerDark shadow-gray-800 text-white' : 'bg-containerLight shadow-gray-300 text-textDark'} font-bold text-xl md:text-2xl p-3 md:p-5 mt-8 rounded-md shadow-md`}>Go to Home</Link>
        </div>
      </Layout>
    </div >
  )
}

export default Error404